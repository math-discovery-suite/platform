import React from "react";
import { CRALayer } from "@math-suite/types";

/** DiscoveryJournal component — see framework doc for full spec */
export function DiscoveryJournal(props: any) {
  return <div>DiscoveryJournal — full implementation in progress</div>;
}
