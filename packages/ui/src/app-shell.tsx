import React from "react";
import { CRALayer } from "@math-suite/types";

/** AppShell component — see framework doc for full spec */
export function AppShell(props: any) {
  return <div>AppShell — full implementation in progress</div>;
}
