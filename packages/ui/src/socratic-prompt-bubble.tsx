import React from "react";
import { CRALayer } from "@math-suite/types";

/** SocraticPromptBubble component — see framework doc for full spec */
export function SocraticPromptBubble(props: any) {
  return <div>SocraticPromptBubble — full implementation in progress</div>;
}
